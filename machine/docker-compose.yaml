services:
  lla-gpu0:
    image: ghcr.io/ggml-org/llama.cpp:server-cuda
    container_name: lla-gpu0
    restart: unless-stopped
    runtime: nvidia
    ulimits:
      memlock:
        soft: -1
        hard: -1
    ports:
      - "11434:8080"
    volumes:
      - ./models:/models:ro
    environment:
      - CUDA_VISIBLE_DEVICES=0
      - LLAMA_ARG_FLASH_ATTN=on
      - LLAMA_ARG_N_GPU_LAYERS=99
      - GGML_CUDA_GRAPH_OPT=1

    command: >
      -m /models/Qwen3.6-35B-A3B-UD-IQ4_XS.gguf
      --host 0.0.0.0
      --port 8080
      --ctx-size 131072
      --cache-type-k q8_0
      --cache-type-v q4_0
      --flash-attn on
      --parallel 1
      -ngl 99 -t 8 -tb 4
      -b 2048 -ub 1024
      --mlock
      --no-mmap
      --spec-type draft-mtp
      --spec-draft-n-max 2
      --spec-draft-type-k q4_0
      --spec-draft-type-v q4_0
    networks:
      - docker_default


  lla-ornith35b:
    image: ghcr.io/ggml-org/llama.cpp:server-cuda
    container_name: lla-ornith35b
    restart: unless-stopped
    runtime: nvidia
    ulimits:
      memlock:
        soft: -1
        hard: -1
    ports:
      - "15433:8080"
    volumes:
      - ./models:/models:ro
    environment:
      - CUDA_VISIBLE_DEVICES=1
      - LLAMA_ARG_FLASH_ATTN=on
      - LLAMA_ARG_N_GPU_LAYERS=99
      - GGML_CUDA_GRAPH_OPT=1

    command: >
      -m /models/deepreinforce-ai_Ornith-1.0-35B-IQ4_XS.gguf
      --host 0.0.0.0
      --port 8080
      --ctx-size 131072
      --cache-type-k q8_0
      --cache-type-v q4_0
      --flash-attn on
      --parallel 1
      -ngl 99 -t 8 -tb 4
      -b 2048 -ub 1024
      --mlock
      --no-mmap
      --jinja
    networks:
      - docker_default

  # ollama:
  #   image: ollama/ollama:latest
  #   container_name: ollama
  #   restart: unless-stopped
  #   # 端口 11434 已被 SGLang 占用，按需启动时通过 Docker 内部网络访问
  #   volumes:
  #     - /opt/ai-workspace/ollama:/root/.ollama
  #   environment:
  #     - CUDA_VISIBLE_DEVICES=0
  #     - OLLAMA_MAX_LOADED_MODELS=2
  #     - OLLAMA_NUM_PARALLEL=2
  #     - OLLAMA_FLASH_ATTENTION=1
  #     - OLLAMA_KV_CACHE_TYPE=q4_0
  #   networks:
  #     - docker_default

networks:
  docker_default:
    external: true
